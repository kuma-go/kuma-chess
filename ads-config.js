/*
 * AdSense publisher and slot identifiers are public identifiers, not secrets.
 * Keep them blank until the site is approved and consent/privacy setup is ready.
 */
window.KUMA_ADS_CONFIG = Object.freeze({
  enabled: false,
  client: "ca-pub-7173992156420612",
  topSlot: "",
  testMode: false,
});
